<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="dto.UserMapper">

	<select id="selectUser" resultType="board.dto.UserDto">
		<![CDATA[
			SELECT
				board_idx,
				title,
				contents,
				creator_id
			FROM
				t_user
			WHERE
				email = #{boardIdx} 
			AND 
			 	 pwd = #{pwd}
		]]>
    </select>
    
   <insert id="insertUser" parameterType="board.dto.UserDto" useGeneratedKeys="true" keyProperty="userIdx">
		<![CDATA[
			INSERT INTO t_user
			(
				id, 
				email, 
				pwd,
				image
			) 
			VALUES 
			(
				#{id}, 
				#{email}, 
				#{pwd}, 
				#{image}			
			)
		]]>
	</insert>
    
    <insert id="insertFileList" parameterType="board.board.dto.BoardFileDto">
		<![CDATA[
			INSERT INTO t_file
			(
				board_idx,
				original_file_name,
				stored_file_path,
				file_size,
				creator_id,
				created_datetime
			)
			VALUES
		]]>
		<foreach collection="list" item="item" separator=",">
			(
				#{item.boardIdx},
				#{item.originalFileName},
				#{item.storedFilePath},
				#{item.fileSize},
				'admin',
				NOW()
			)
		</foreach>
	</insert>
	
	<select id="selectFileList" parameterType="int" resultType="board.board.dto.BoardFileDto">
		<![CDATA[
			SELECT
				idx,
				board_idx,
				original_file_name,
				FORMAT(ROUND(file_size / 1024), 0) AS file_size
			FROM
				t_file
			WHERE
				board_idx = #{boardIdx}
				AND deleted_yn = 'N'
		]]>
	</select>
    
    <select id="selectFileInformation" parameterType="map" resultType="board.board.dto.BoardFileDto">
		<![CDATA[
			SELECT
				original_file_name,
				stored_file_path,
				file_size
			FROM
				t_file
			WHERE
				idx = #{idx}
				AND board_idx = #{boardIdx}
				AND deleted_yn = 'N'
		]]>
	</select>
    
</mapper>